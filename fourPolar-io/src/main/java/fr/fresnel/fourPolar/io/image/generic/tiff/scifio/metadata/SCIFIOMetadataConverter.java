package fr.fresnel.fourPolar.io.image.generic.tiff.scifio.metadata;

import fr.fresnel.fourPolar.core.image.generic.IMetadata;
import io.scif.ImageMetadata;

/**
 * Using this class, we can convert the SCIFIO Tiff metadata to {@link IMetadata}.
 */
public class SCIFIOMetadataConverter {
    public static void convertFrom(ImageMetadata SCIFIOMetadata) {
        
    }

    public static void convertTo(IMetadata metadata) {
        
    }
}